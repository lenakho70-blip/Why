# Optional inbound auth. If set, n8n must send Authorization: Bearer <RENDER_TOKEN>.
# Currently the workflow sends NO auth header, so leave this UNSET.
RENDER_TOKEN=

# Railway sets PORT automatically; default 8080 if absent.
PORT=8080
